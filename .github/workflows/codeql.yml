name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 9 1 * *'  # monthly, 1st 09:00 UTC

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  analyze:
    uses: Upstream-Intelligence/.github/.github/workflows/reusable-codeql.yml@main
