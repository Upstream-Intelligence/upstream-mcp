{
  "name": "@upstream-health/mcp-server",
  "version": "1.7.1",
  "description": "MCP server for Upstream healthcare denial intelligence",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "upstream-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
